def accss_resource(thread_name):
    sem.acquire()
    print(f"{thread_name}")
    time.sleep(2)
    print(f"{thread_name}")


threads = []
for i in range(5):
    t = threading.Thread(target=access_resource, args=(f"Thread-{i}",))
    threads.append(t)
    t.start()

for t in threads:
    t.join()